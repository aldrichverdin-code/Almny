@{
    ViewData["Title"] = "Sign In";
}
<div class="auth-wrap">
    <div class="auth-box">
        <div class="auth-header">
            <div class="auth-logo"><span>A</span>M<span>T</span>C</div>
            <div class="auth-subtitle">Welcome</div>
        </div>
        <div class="auth-body">
            <!-- Mensaje de error (ejemplo) -->
            @* <div style="background: #c0392b; color: #fff; padding: 0.75rem; border-radius: 8px; font-size: 0.85rem; margin-bottom: 1rem;">
                Invalid email or password
            </div> *@

            <!-- Mensaje de éxito (ejemplo - comentado) -->
            <!--
                <div style="background: #27ae60; color: #fff; padding: 0.75rem; border-radius: 8px; font-size: 0.85rem; margin-bottom: 1rem;">
                    Successfully registered! Please sign in.
                </div>
                -->

            <form>
                <div class="form-group">
                    <label class="form-label">Employee Number</label>
                    <input type="number" name="email" class="form-input" placeholder="12345" required />
                </div>
                <div class="form-group">
                    <label class="form-label">Password</label>
                    <input type="password" name="password" class="form-input" placeholder="••••••••" required />
                </div>
                <br />
                <button type="submit" class="btn btn-primary" style="width:100%;">Sign In →</button>
            </form>
            <div class="auth-switch">
                Don't have an account? <a href="@Url.Action("Register", "Auth")">Join Free</a>
            </div>
        </div>
    </div>
</div>